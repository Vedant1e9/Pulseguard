# PatientTriage.ai: runtime dependencies
#
# Pinned to lower bounds that the code is verified against. The versions this
# build was developed and evaluated on are recorded in MODEL_CARD.md.

# --- Core ---
streamlit>=1.40
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
PyYAML>=6.0

# --- Gradient-boosted models ---
# Both are NaN-native, which is why missing vitals never need imputing.
xgboost>=2.0
lightgbm>=4.0

# --- Explainability ---
# TreeSHAP for per-patient attribution. Without it the explanation layer falls
# back to counterfactual perturbation, which is slower but model-agnostic, so
# the system degrades rather than breaking.
shap>=0.44

# --- Testing ---
pytest>=8.0
pytest-cov>=5.0          # coverage figures reported in HEADLINE_NUMBERS.md

# --- Optional: spoken handover intake ---
# Neither is required. Without them the Spoken handover page still runs its
# deterministic clinical parser on typed or pasted text, and every other page
# is unaffected.
#
#   faster-whisper>=1.0     # on-device speech to text, no audio leaves the machine
#   openai-whisper>=20231117 # alternative on-device speech to text
#   anthropic>=0.40         # LLM field extraction, needs ANTHROPIC_API_KEY
#   openai>=1.40            # LLM field extraction and Whisper API, needs OPENAI_API_KEY
#
# Set PT_ALLOW_CLOUD_AUDIO=0 to forbid audio leaving the machine under any
# configuration, regardless of which keys are present.
